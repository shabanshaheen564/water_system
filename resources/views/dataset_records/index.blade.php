@extends('layouts.app')
@section('title', __('Records'))
@section('content')
<div class="mx-auto max-w-7xl p-4 sm:p-6 lg:p-8">
    <div class="mb-6 flex flex-wrap items-center justify-between gap-4"><div><h2 class="text-xl font-semibold leading-[1.5] text-ink">{{ $dataset->display_name }} — {{ __('Records') }}</h2><p class="mt-1 text-sm text-ink-secondary" dir="ltr">{{ $dataset->name }}</p></div>@can('datasets.create')<a href="{{ route('datasets.records.create', $dataset) }}" class="btn-motion rounded-md bg-brand-600 px-4 py-2 text-sm font-medium text-white hover:bg-brand-700">{{ __('Add Record') }}</a>@endcan</div>
    @if(session('success'))<div class="mb-4 border border-success bg-success-surface p-4 text-sm text-success">{{ session('success') }}</div>@endif
    @if($errors->any())<div class="mb-4 border border-danger bg-danger-surface p-4 text-sm text-danger">{{ $errors->first() }}</div>@endif
    <form method="GET" class="mb-4 flex gap-2"><input name="search" value="{{ $search }}" placeholder="{{ __('Search records...') }}" class="input-institutional min-w-0 flex-1 text-sm"><button class="rounded-md bg-ink px-4 py-2 text-sm font-medium text-white">{{ __('Search') }}</button></form>
    <div data-enter class="card-institutional overflow-x-auto"><table class="min-w-full"><thead class="bg-surface-1"><tr><th class="border-b border-border px-4 py-3 text-start text-xs font-medium text-ink-muted">ID</th>@foreach($dataset->fields as $field)<th class="border-b border-border px-4 py-3 text-start text-xs font-medium text-ink-muted">{{ $field->display_name }}</th>@endforeach<th class="border-b border-border px-4 py-3 text-start text-xs font-medium text-ink-muted">{{ __('Actions') }}</th></tr></thead><tbody>@forelse($records as $record)<tr class="border-b border-border last:border-b-0"><td class="px-4 py-3 text-sm font-mono" dir="ltr">{{ $record->identifier_value ?? $record->id }}</td>@foreach($dataset->fields as $field)<td class="px-4 py-3 text-sm text-ink-secondary">{{ is_bool($record->values[$field->name] ?? null) ? (($record->values[$field->name] ?? false) ? __('Yes') : __('No')) : ($record->values[$field->name] ?? '—') }}</td>@endforeach<td class="whitespace-nowrap px-4 py-3 text-sm">@can('datasets.update')<a class="me-3 text-brand-600 hover:text-brand-700" href="{{ route('datasets.records.edit', [$dataset, $record]) }}">{{ __('Edit') }}</a>@endcan @can('datasets.delete')<form class="inline" method="POST" action="{{ route('datasets.records.destroy', [$dataset, $record]) }}">@csrf @method('DELETE')<button onclick="return confirm('{{ __('Delete this record?') }}')" class="text-danger hover:underline">{{ __('Delete') }}</button></form>@endcan</td></tr>@empty<tr><td colspan="{{ $dataset->fields->count()+2 }}" class="px-4 py-10 text-center text-sm text-ink-muted">{{ __('No records found.') }}</td></tr>@endforelse</tbody></table></div>
    <div class="mt-4">{{ $records->links() }}</div>
</div>
@endsection