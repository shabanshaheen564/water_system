@extends('layouts.app')
@section('title', __('Edit Record'))
@section('content')
<div class="p-4 sm:p-6 lg:p-8 max-w-4xl mx-auto"><div class="mb-6"><h1 class="text-2xl font-bold text-gray-900">{{ __('Edit Record') }}</h1><p class="text-gray-600 mt-1">{{ $dataset->display_name }} — {{ $record->identifier_value ?? $record->id }}</p></div>
@if($errors->any())<div class="mb-4 rounded-md bg-red-50 p-4 text-sm text-red-800"><ul class="list-disc pl-5">@foreach($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif
<form method="POST" action="{{ route('datasets.records.update', [$dataset, $record]) }}" class="bg-white rounded-lg border border-gray-200 shadow-sm p-6 space-y-5">@csrf @method('PUT')
@foreach($dataset->fields as $field)@php($value = old('values.'.$field->name, $record->values[$field->name] ?? $field->default_value))<div><label class="block text-sm font-medium text-gray-700 mb-1">{{ $field->display_name }} @if($field->is_required)<span class="text-red-600">*</span>@endif</label>@if($field->is_identifier)<input type="text" value="{{ $value }}" disabled class="w-full rounded-md border-gray-300 bg-gray-100">@elseif($field->data_type === 'boolean')<label class="inline-flex items-center"><input type="hidden" name="values[{{ $field->name }}]" value="0"><input type="checkbox" name="values[{{ $field->name }}]" value="1" @checked($value) class="rounded border-gray-300"><span class="ml-2 text-sm">{{ __('Yes') }}</span></label>@elseif($field->data_type === 'text')<textarea name="values[{{ $field->name }}]" class="w-full rounded-md border-gray-300">{{ $value }}</textarea>@else<input name="values[{{ $field->name }}]" type="{{ in_array($field->data_type, ['integer','decimal']) ? 'number' : ($field->data_type === 'date' ? 'date' : ($field->data_type === 'datetime' ? 'datetime-local' : 'text')) }}" @if($field->data_type === 'decimal') step="any" @endif value="{{ $value }}" class="w-full rounded-md border-gray-300">@endif</div>@endforeach
<div class="flex gap-3"><button class="px-4 py-2 bg-blue-600 text-white rounded-md">{{ __('Save Changes') }}</button><a href="{{ route('datasets.records.index', $dataset) }}" class="px-4 py-2 border rounded-md">{{ __('Cancel') }}</a></div></form></div>
@endsection
